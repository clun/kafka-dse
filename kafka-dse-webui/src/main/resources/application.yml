# ----------------------------------------------------------
# Spring Boot extra Config
# ----------------------------------------------------------
spring:
  application:
    name: WebUI for reactive streams
  messages:
    basename: messages
server:
  port: 8082
   
# ----------------------------------------------------------
# Connectivity to DataStax Enterprise
# ----------------------------------------------------------
dse:
  contactPoints: 127.0.0.1
  port: 9042
  localdc: DC1
  username:
  password:
  keyspace: demo_kafka
  